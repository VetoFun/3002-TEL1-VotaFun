{
    "buildpacks": [
        {
            "url": "heroku/python"
        }
    ],
    "environments": {
        "test": {
            "env": {
                "APP_SETTINGS": "src.config.TestingConfig"
            },
            "scripts": {
                "test-setup": "cd backend && pip install -r requirements.txt",
                "test": "pytest tests/ -v"
            }
        }
    }
}
